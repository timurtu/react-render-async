{
  "scripts": {
    "install": "cd example && npm install react-render-async --save",
    "start": "cd example && npm start",
    "reboot": "babel src --out-file lib/index.js && cd lib && npm version patch && npm publish && cd ../example && npm install react-render-async --save && npm start"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1"
  }
}
